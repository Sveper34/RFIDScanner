<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_search">

    <fragment
        android:id="@+id/nav_search"
        android:name="com.arktika.rfidscanner.ui.home.HomeFragment"
        android:label="@string/rfid_scaner_title"
        tools:layout="@layout/fragment_search" />

    <fragment
        android:id="@+id/nav_find_replace_rfid"
        android:name="com.arktika.rfidscanner.ui.gallery.GalleryFragment"
        android:label="@string/rfid_replacement"
        tools:layout="@layout/fragment_find_replace" />

    <fragment
        android:id="@+id/nav_link_rfid"
        android:name="com.arktika.rfidscanner.ui.slideshow.SlideshowFragment"
        android:label="@string/menu_slideshow"
        tools:layout="@layout/fragment_link" />
</navigation>